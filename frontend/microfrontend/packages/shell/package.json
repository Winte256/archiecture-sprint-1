{
  "name": "microfrontend_shell",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider webpack-cli serve" ,
    "build": "NODE_OPTIONS=--openssl-legacy-provider webpack --mode production",
    "serve": "serve dist -p 3000",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eventemitter3": "^5.0.1"
  }
}
